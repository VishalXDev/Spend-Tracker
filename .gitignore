# Node modules (don't commit them)
node_modules/

# Next.js build output (don't commit it)
.next/

# Environment files (don't commit them, they may contain sensitive data)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Lock files (optional, but typically not needed for frontend or when managing dependencies via npm/yarn)
package-lock.json
yarn.lock

# TypeScript configuration file (optional, can be included if needed)
tsconfig.json

# Logs (don't commit them)
*.log
logs/

# IDE and OS-specific files (don't commit them)
.vscode/
.idea/
.DS_Store
Thumbs.db

# Coverage (for testing tools like Jest)
coverage/

# Temporary files (don't commit them)
*.swp
*.swo
